% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value-of-information.R
\name{decide_evpi}
\alias{decide_evpi}
\title{Expected value of perfect information from a manifest}
\usage{
decide_evpi(
  manifest,
  candidates,
  utility = NULL,
  grounding = NULL,
  safe_action = NULL,
  draws_key = "yield_draws",
  ...
)
}
\arguments{
\item{manifest}{An \code{orchestra_manifest} S7 object (or any S7 object exposing
the contract's \code{outputs} / \code{metadata} / \code{summary} properties).}

\item{candidates}{A numeric vector (or list) of candidate actions.}

\item{utility}{A function \verb{function(action, draws_column)} returning a
numeric utility per draw, or \code{NULL} when each manifest column is already
the utility of the corresponding candidate.}

\item{grounding}{Optional override for the manifest's own grounding token.}

\item{safe_action}{The action the baseline decision abstains to; defaults to
the first candidate.}

\item{draws_key}{The \code{metadata} key under which a draws matrix is sought
when the \code{outputs} slot is empty.}

\item{...}{Further arguments passed to the decision core (e.g. \code{constraint},
\code{min_ess}, \code{ess}, \code{decisive_prob}).}
}
\value{
A \link{valuation} of \code{type = "evpi"}. Its \code{components} carry the two
expectations the value is the difference of, the draw count and the number
of feasible candidates; \verb{@baseline} carries the decision current evidence
supports.
}
\description{
Prices what it would be worth to learn the latent state exactly before
acting. Given the same manifest, candidate set and utility that
\code{\link[=decide_from_manifest]{decide_from_manifest()}} takes, \code{decide_evpi()} returns
\deqn{EVPI = E_\theta[\max_a U(a, \theta)] - \max_a E_\theta[U(a, \theta)],}
the difference between deciding with the state known and deciding with only
its posterior. It is the ceiling on the value of any study, trial or
measurement addressing that state: no finite sample can be worth more, which
is what makes it the natural first screen before an experiment is designed.
}
\details{
The expectation runs over the feasible candidates only, so a hard constraint
passed through \code{...} prices the information available \emph{within} the actions
the grower or the agency can actually take, not within a wider hypothetical
set.

Grounding rides from the producer exactly as it does for a decision: an
\verb{[unverified]} manifest returns an abstained \code{valuation} with
\code{abstain_reason = "input_ungrounded"} and \code{value = NA_real_}. The other
blocking reasons are \code{no_feasible_action}, \code{low_ess} and
\code{degenerate_utility}. An \code{insufficient_evidence} baseline decision is \emph{not}
blocking: a too-close-to-call decision is precisely where information pays,
and the indecisive decision is returned in \verb{@baseline}.
}
\examples{
set.seed(1L)
rates <- seq(0, 200, by = 25)
demo_manifest <- S7::new_class(
  "demo_manifest",
  properties = list(
    outputs         = S7::new_property(S7::class_any, default = NULL),
    metadata        = S7::new_property(S7::class_list, default = list()),
    summary         = S7::new_property(S7::class_any, default = NULL),
    run_id          = S7::new_property(S7::class_character, default = ""),
    emitter_package = S7::new_property(S7::class_character, default = "")
  )
)
ymax <- rnorm(800L, 5.5, 0.6)
yld  <- vapply(rates,
               function(r) 3 + (ymax - 3) * (1 - exp(-r / 80)),
               numeric(800L))
m <- demo_manifest(
  metadata = list(yield_draws = yld, grounding = grounding_grounded())
)
decide_evpi(m, candidates = rates,
            utility = function(r, y) 300 * y - 1.2 * r)
}
\references{
Raiffa, H. and Schlaifer, R. (1961) \emph{Applied Statistical Decision Theory}.
Boston: Harvard University Graduate School of Business Administration.
}
\seealso{
\code{\link[=decide_evsi]{decide_evsi()}} for the value of one finite sample;
\code{\link[=decide_from_manifest]{decide_from_manifest()}} for the decision the value is measured against.

Other value: 
\code{\link{decide_bcr}()},
\code{\link{decide_evsi}()},
\code{\link{valuation}()}
}
\concept{value}
