% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benefit-cost.R
\name{bcr_crrdc}
\alias{bcr_crrdc}
\title{CRRDC-guideline benefit-cost ratio}
\usage{
bcr_crrdc(
  benefits,
  costs,
  counterfactual,
  attribution,
  adoption,
  counterfactual_label = NULL,
  discount_central = 0.05,
  discount_sensitivity = c(0.03, 0.07),
  base_year = NULL,
  grounding = NULL
)
}
\arguments{
\item{benefits}{A \code{data.frame(year, value)}: the (gross, pre-counterfactual)
annual real benefit stream.}

\item{costs}{A \code{data.frame(year, value)}: the annual real cost stream. Not
adjusted by \code{counterfactual}, \code{attribution} or \code{adoption}.}

\item{counterfactual}{Required, no default. Either a \code{data.frame(year, value)} of benefits under the counterfactual, spanning every year
\code{benefits} covers, subtracted from \code{benefits}; or the string
\code{"already_net"} declaring \code{benefits} is already incremental, in which
case \code{counterfactual_label} is also required.}

\item{attribution}{Required, no default. A single number in \verb{(0, 1]}: the
share of the incremental benefit attributable to this investment.}

\item{adoption}{Required, no default. A \code{data.frame(year, uptake)} with
\code{uptake} in \verb{[0, 1]}, spanning every year \code{benefits} covers, applied
multiplicatively to the net, attributed benefit stream.}

\item{counterfactual_label}{A single character string naming the declared
counterfactual. Required when \code{counterfactual = "already_net"};
optional (used only for reporting) when \code{counterfactual} is a
\code{data.frame}.}

\item{discount_central}{The central real discount rate, as a proportion
(CRRDC's Australian rural R&D standard is \code{0.05}).}

\item{discount_sensitivity}{A numeric vector of sensitivity discount rates
reported alongside \code{discount_central} (CRRDC's standard band is
\code{c(0.03, 0.07)}).}

\item{base_year}{The undiscounted period-zero year; defaults to the
earliest year across \code{benefits} and \code{costs}.}

\item{grounding}{Optional grounding token(s) for the input facts,
combined worst-case with \code{\link[=combine_grounding]{combine_grounding()}}; defaults to
\code{\link[=grounding_unverified]{grounding_unverified()}} when not supplied.}
}
\value{
A \code{decider_bcr} object: \code{rates} (a \code{data.frame} with one row per
discount rate and columns \code{rate_label}, \code{rate}, \code{pv_benefits},
\code{pv_costs}, \code{npv}, \code{bcr}), \code{counterfactual_label}, \code{attribution},
\code{adoption}, \code{base_year}, \code{grounding}, \code{abstained} and \code{abstain_reason}.
}
\description{
Discounts a benefit stream and a cost stream to a benefit-cost ratio in the
shape a Council of Rural Research and Development Corporations (CRRDC)
impact assessment reports, with the four inputs the guidelines make
mandatory: an explicit counterfactual, an explicit attribution share, an
adoption profile with lags, and a discount-rate sensitivity band around the
central rate. Where \code{\link[=decide_bcr]{decide_bcr()}} leaves those assumptions to the caller,
\code{bcr_crrdc()} refuses to construct a ratio without all four -- a
benefit-cost ratio that implicitly assumes no counterfactual, full
attribution and instant 100\% uptake is exactly what the guidelines forbid.
}
\details{
\code{counterfactual} states what would have happened without the investment.
Supply a \code{data.frame(year, value)} of benefits under the counterfactual
scenario, spanning every year \code{benefits} covers, and it is subtracted from
\code{benefits} year by year before attribution and adoption are applied.
Alternatively, when \code{benefits} already IS the incremental (net-of-
counterfactual) stream, pass \code{counterfactual = "already_net"} and name the
declared counterfactual in \code{counterfactual_label} -- the label is required
in this branch because a net stream carries no other record of what it is
net of.

\code{attribution} is the proportion, in \verb{(0, 1]}, of the incremental benefit
this investment alone caused, net of co-funders, extension effort and
other contributing causes. \code{adoption} is the uptake profile, a
\code{data.frame(year, uptake)} with \code{uptake} in \verb{[0, 1]}, applied
multiplicatively to the net, attributed benefit stream; it must cover
every year \code{benefits} covers, so an adoption lag is always stated rather
than assumed away. Costs are not adjusted by attribution or adoption --
the investment cost is what was actually spent, not a share of a benefit.

The ratio is reported at \code{discount_central} (the CRRDC standard is 5\% for
Australian rural R&D) and, side by side, at every rate in
\code{discount_sensitivity} (the CRRDC standard band is 3\% and 7\%), so a reader
sees how sensitive the ratio is to the one assumption a guideline mandates
testing. \code{base_year} (period \code{t = 0}, undiscounted) defaults to the
earliest year across \code{benefits} and \code{costs}, so an investment whose costs
precede its first benefit year is discounted from the year spending began.

Grounding rides the same Independent Oracle Principle firewall as the rest
of decideR: an un-grounded input (the default when \code{grounding} is not
supplied) returns an abstained \code{decider_bcr} with \code{abstain_reason = "input_ungrounded"} and every rate's \code{pv_benefits}/\code{pv_costs}/\code{npv}/\code{bcr}
\code{NA}, never a ratio a funding submission would quote. A discounted cost
base that is not strictly positive at the central rate (a zero or
negative cost stream) abstains with \code{abstain_reason = "undefined_ratio"}
for the same reason.
}
\examples{
# A ten-year, evenly-adopted, half-attributed investment. The benefit
# stream is already declared incremental against a named counterfactual.
bens <- data.frame(year = 2020:2029, value = rep(500, 10))
csts <- data.frame(year = 2020:2029, value = rep(1000, 10))
up   <- data.frame(year = 2020:2029, uptake = rep(1, 10))
bcr_crrdc(bens, csts, counterfactual = "already_net",
          counterfactual_label = "no new variety released",
          attribution = 0.5, adoption = up,
          grounding = grounding_grounded())
}
\seealso{
\code{\link[=decide_bcr]{decide_bcr()}} for a benefit-cost ratio without the CRRDC
mandatory inputs.

Other value: 
\code{\link{decide_bcr}()},
\code{\link{decide_evpi}()},
\code{\link{decide_evsi}()},
\code{\link{valuation}()}
}
\concept{value}
