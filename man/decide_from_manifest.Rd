% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest-tail.R
\name{decide_from_manifest}
\alias{decide_from_manifest}
\title{Turn an orchestra manifest into a decision}
\usage{
decide_from_manifest(
  manifest,
  candidates,
  utility = NULL,
  grounding = NULL,
  safe_action = NULL,
  draws_key = "yield_draws",
  ...
)
}
\arguments{
\item{manifest}{An \code{orchestra_manifest} S7 object (or any S7 object exposing
the contract's \code{outputs} / \code{metadata} / \code{summary} properties).}

\item{candidates}{A numeric vector of candidate actions, one per column of
the manifest's draws matrix; or a numeric vector the \code{utility} indexes
freely when the draws are a single posterior vector.}

\item{utility}{A function \verb{function(action, draws_column)} returning a numeric
utility per draw, or \code{NULL} to treat each manifest column as the utility of
the corresponding candidate directly (the case a producer has already
evaluated the loss per candidate).}

\item{grounding}{Optional override for the manifest's own grounding token;
defaults to the token the manifest carries, read worst-case.}

\item{safe_action}{The action returned on abstention; defaults to the first
candidate (the status-quo convention for an ordered grid).}

\item{draws_key}{The \code{metadata} key under which a draws matrix is sought when
the \code{outputs} slot is empty.}

\item{...}{Further arguments passed to the decision core (e.g. \code{min_ess},
\code{ess}, \code{decisive_prob}, \code{safe_label}, \code{method}, \code{metadata}).}
}
\value{
A \link{decision} object whose \code{inputs} record the manifest's \code{run_id} and
\code{emitter_package} for provenance.
}
\description{
The manifest-native pipeline tail. Given an \code{orchestra_manifest} emitted by
any ORCHESTRA member -- a PESTO ensemble inversion, a kernR causal test, a
flexyBayes posterior -- \code{decide_from_manifest()} extracts the predictive
draws and the producer's grounding token from the manifest, then routes them
through the same expected-utility engine \code{\link[=decide]{decide()}} uses. It is the standard
closer of the crop pipeline: an upstream member's inference becomes a
loss-optimal, grounding-aware action without the caller re-assembling the
draws by hand.
}
\details{
The grounding label rides from the producer through the decision: a manifest
whose payload is \verb{[unverified]} forces an abstention to \code{safe_action},
regardless of how attractive the draws look. This is the Independent Oracle
Principle firewall applied at the pipeline boundary -- decideR never mints a
confident recommendation on an upstream fact the producer did not ground.

decideR does not depend on the contract package; the manifest is read through
its public S7 properties (\code{outputs}, \code{metadata}, \code{summary}). The draws are
sought in the \code{outputs} slot first, then under \code{metadata[[draws_key]]}, so the
tail consumes any conforming producer rather than special-casing each one.
}
\examples{
set.seed(1L)
rates <- seq(0, 200, by = 25)
# In production the manifest is a real `orchestra_manifest` S7 object from
# the composition layer. Here, a minimal S7 stand-in carrying the same
# public properties the tail reads (`outputs`, `metadata`, `summary`,
# `run_id`, `emitter_package`) -- an S3 list is NOT readable by the tail,
# which duck-types through S7 property access.
demo_manifest <- S7::new_class(
  "demo_manifest",
  properties = list(
    outputs         = S7::new_property(S7::class_any, default = NULL),
    metadata        = S7::new_property(S7::class_list, default = list()),
    summary         = S7::new_property(S7::class_any, default = NULL),
    run_id          = S7::new_property(S7::class_character, default = ""),
    emitter_package = S7::new_property(S7::class_character, default = "")
  )
)
ymax <- rnorm(1500L, 5.5, 0.3)
yld  <- vapply(rates,
               function(r) 3 + (ymax - 3) * (1 - exp(-r / 80)),
               numeric(1500L))
m <- demo_manifest(
  metadata = list(yield_draws = yld, grounding = grounding_grounded())
)
decide_from_manifest(m, candidates = rates,
                     utility = function(r, y) 300 * y - 1.2 * r)
}
\seealso{
\code{\link[=decide_rate_from_manifest]{decide_rate_from_manifest()}} for the agronomic convenience;
\code{\link[=decide]{decide()}} for the underlying engine.

Other manifest: 
\code{\link{decide_rate_from_manifest}()}
}
\concept{manifest}
