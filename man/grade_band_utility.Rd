% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loss-grade-band.R
\name{grade_band_utility}
\alias{grade_band_utility}
\title{Build a decide() utility from a grade-band schedule}
\usage{
grade_band_utility(value, quality_shift = NULL, cost = 0)
}
\arguments{
\item{value}{A \link{grade_band_value} schedule.}

\item{quality_shift}{A function \verb{function(action, theta)} returning the
realised quality outcome under the action, or \code{NULL} for the additive
default \code{theta + action}.}

\item{cost}{A function \verb{function(action)} returning the action's cost, or a
single numeric per-unit cost applied as \code{cost * action}, or \code{0} for no
cost.}
}
\value{
A function \verb{function(action, theta)} suitable as the \code{utility}
argument to \code{\link[=decide]{decide()}}.
}
\description{
Turns a \link{grade_band_value} schedule into a \code{utility(action, theta)} function
the decision engine consumes. \code{theta} is a posterior draw of the quality
outcome (grain protein, say); the action shifts that outcome through
\code{quality_shift}, a function \verb{function(action, theta)} returning the realised
quality under the action. The utility of an action at a draw is the band
payoff of the shifted outcome, less an optional per-unit action cost. Because
the payoff is a step function, the expected utility decideR averages over the
draws is governed by each band's posterior probability -- exactly the
threshold economics a quality-graded decision faces.
}
\details{
When \code{quality_shift} is left \code{NULL} the action is taken to \emph{be} the quality
outcome offset added to \code{theta} (the common case where an input nudges the
outcome additively), so \code{quality = theta + action}. Supply a \code{quality_shift}
for any non-additive response (a saturating dose-response, say).
}
\examples{
v <- grade_band_value(c(10.5, 11.5, 13), c(300, 330, 360, 400))
# A nitrogen rate lifts protein additively; protein costs nothing but N does.
u <- grade_band_utility(v, quality_shift = function(rate, protein)
                          protein + 0.01 * rate, cost = 1.2)
set.seed(1L)
protein <- rnorm(2000L, mean = 11.0, sd = 0.6)
decide(protein, u, candidates = seq(0, 150, by = 25),
       grounding = grounding_grounded(), safe_action = 0)
}
\seealso{
\code{\link[=grade_band_value]{grade_band_value()}} for the schedule; \code{\link[=decide]{decide()}} for the engine.

Other loss: 
\code{\link{grade_band_value}()}
}
\concept{loss}
