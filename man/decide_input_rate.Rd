% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference-decisions.R
\name{decide_input_rate}
\alias{decide_input_rate}
\title{Choose a crop input rate by expected profit}
\usage{
decide_input_rate(
  yield_draws,
  rates,
  price_grain,
  price_input,
  grounding = grounding_unverified(),
  safe_rate = 0,
  decisive_prob = 0.6,
  ...
)
}
\arguments{
\item{yield_draws}{A numeric matrix of posterior yield draws, one column per
candidate rate (rows are draws).}

\item{rates}{A numeric vector of candidate input rates, one per column of
\code{yield_draws}.}

\item{price_grain}{Grain price per unit yield.}

\item{price_input}{Input price per unit rate.}

\item{grounding}{One or more grounding tokens for the yield evidence;
combined worst-case.}

\item{safe_rate}{The fallback rate on abstention (default \code{0}, the
no-input status quo).}

\item{decisive_prob}{Minimum posterior probability that the leading rate is
best (see \code{\link[=decide]{decide()}}).}

\item{...}{Further arguments passed to the decision core: \code{min_ess}, \code{ess},
\code{constraint} (a \verb{function(rate)} feasibility check), \code{safe_label},
\code{method}, \code{metadata}, \code{inputs}. Any other name stops with an error rather
than being silently discarded.}
}
\value{
A \link{decision} object whose \code{action} is the chosen rate.
}
\description{
A reference decision for the agronomic domain. Given posterior draws of yield
at each candidate input rate -- the kind of object a mechanistic-simulator
inversion (PESTO) or a causal test (TACI) emits -- it selects the rate that
maximises expected profit, \eqn{E[p_y\,y(r) - p_r\,r]}, where \eqn{p_y} is the
grain price and \eqn{p_r} the input price. It abstains to \code{safe_rate} when the
yield evidence is un-grounded or the leading rate is not decisive.
}
\examples{
set.seed(1L)
rates <- seq(0, 200, by = 40)
# diminishing-returns yield response with posterior noise
mu <- 3 + 2.5 * (1 - exp(-rates / 80))
yield_draws <- sapply(mu, function(m) rnorm(1500L, m, 0.3))
decide_input_rate(yield_draws, rates, price_grain = 300, price_input = 1.2,
                  grounding = grounding_grounded())
}
\seealso{
\code{\link[=decide]{decide()}} for the underlying engine; \code{\link[=decide_position_size]{decide_position_size()}} for
the trading counterpart.

Other decisions: 
\code{\link{decide}()},
\code{\link{decide_position_size}()},
\code{\link{decision}()},
\code{\link{is_grounded}()}
}
\concept{decisions}
