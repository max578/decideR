% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benefit-cost.R
\name{decide_bcr}
\alias{decide_bcr}
\title{Benefit-cost ratio of a research investment}
\usage{
decide_bcr(
  benefits = NULL,
  costs,
  discount_rate,
  horizon = NULL,
  grounding = NULL,
  manifest = NULL,
  draws_key = "benefit_draws",
  probs = c(0.05, 0.5, 0.95),
  metadata = list()
)
}
\arguments{
\item{benefits}{The benefit stream: a numeric vector, one value per period
from \code{t = 0}, or a numeric matrix of posterior draws (rows) by periods
(columns). May be \code{NULL} when \code{manifest} supplies it.}

\item{costs}{The cost stream, a numeric vector, one value per period from
\code{t = 0}.}

\item{discount_rate}{The real discount rate per period, as a proportion
(e.g. \code{0.05} for five per cent).}

\item{horizon}{The number of periods, \verb{t = 0, ..., horizon - 1}; defaults to
the longer of the two streams.}

\item{grounding}{Optional grounding token(s) for the streams, combined
worst-case; defaults to the manifest's own token, or to
\code{grounding_unverified()} when no manifest is supplied.}

\item{manifest}{Optional \code{orchestra_manifest} S7 object supplying the
benefit stream and its grounding.}

\item{draws_key}{The \code{metadata} key under which the benefit draws are sought
when the manifest's \code{outputs} slot is empty.}

\item{probs}{Quantiles at which the posterior ratio is summarised when the
benefit stream arrives as draws.}

\item{metadata}{A named list of free-form metadata.}
}
\value{
A \link{valuation} of \code{type = "bcr"} whose \code{value} is the ratio and whose
\code{components} carry the discounted streams, the two present values and the
net present value.
}
\description{
Discounts a stream of benefits and a stream of costs at an explicit rate
over an explicit horizon and reports the components an impact assessment is
written from -- the two streams, the present value of each, the net present
value, and the ratio:
\deqn{PV = \sum_{t=0}^{H-1} \frac{x_t}{(1 + r)^t}, \quad
      NPV = PV_B - PV_C, \quad BCR = PV_B / PV_C.}
This is the CRRDC-style reporting set an Australian rural research and
development impact assessment closes with. decideR encodes no guideline
default for the rate or the horizon: both are stated by the assessment the
analysis serves and are the caller's to supply, so a ratio can never be
quoted against an assumption its author did not make.
}
\details{
Period \code{t = 0} is the present and is not discounted. A stream given as
end-of-period cash flows is expressed by leading it with a zero, as in the
worked example below. Streams shorter than the horizon are zero-padded;
a stream longer than the horizon is an error rather than a silent
truncation.

When \code{manifest} is supplied, the benefit stream and its grounding token are
read from it through the same duck-typed contract accessors the decision
tail uses (\code{outputs}, then \code{metadata[[draws_key]]}; grounding from
\code{metadata$grounding} or \code{summary$metrics$grounding}), with draws in rows and
periods in columns. The point estimate then uses the posterior mean stream,
and the posterior distribution of the ratio is summarised in
\code{components$bcr_quantiles}. An \verb{[unverified]} manifest returns an abstained
valuation, never a ratio.
}
\examples{
# A two-period investment: 100 spent now, 210 returned next period, at a
# five per cent discount rate. PV(costs) = 100, PV(benefits) = 210/1.05 =
# 200, so NPV = 100 and BCR = 2.
v <- decide_bcr(benefits = c(0, 210), costs = c(100, 0),
                discount_rate = 0.05,
                grounding = grounding_grounded())
v@value
v@components$npv

# The same investment on an un-grounded benefit stream prices out as an
# abstention rather than as a ratio.
decide_bcr(benefits = c(0, 210), costs = c(100, 0), discount_rate = 0.05)
}
\seealso{
\code{\link[=decide_evsi]{decide_evsi()}} for the value of the next study rather than of the
whole programme.

Other value: 
\code{\link{decide_evpi}()},
\code{\link{decide_evsi}()},
\code{\link{valuation}()}
}
\concept{value}
