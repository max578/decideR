% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loss-grade-band.R
\name{grade_band_value}
\alias{grade_band_value}
\title{A grade-band value schedule (threshold-economics loss)}
\usage{
grade_band_value(breaks, values, right = FALSE, labels = NULL)
}
\arguments{
\item{breaks}{A strictly increasing numeric vector of band edges (e.g. the
protein thresholds \code{c(10.5, 11.5, 13)}).}

\item{values}{A numeric vector of band payoffs, of length
\code{length(breaks) + 1}, ordered from the lowest band to the highest.}

\item{right}{Logical; when \code{FALSE} (default) bands are \verb{[lower, upper)} so an
outcome on a break clears into the higher band; when \code{TRUE} bands are
\verb{(lower, upper]}.

[lower, upper)\verb{so an outcome on a break clears into the higher band; when}TRUE\verb{bands are}(lower, upper]: R:lower,\%20upper)\%60\%20so\%20an\%0A\%20\%20outcome\%20on\%20a\%20break\%20clears\%20into\%20the\%20higher\%20band;\%20when\%20\%60TRUE\%60\%20bands\%20are\%0A\%20\%20\%60(lower,\%20upper}

\item{labels}{Optional character labels for the bands, of length
\code{length(values)}; defaults to formatted interval strings.}
}
\value{
A function of one numeric argument returning the band payoff for each
element, with class \code{grade_band_value} and the schedule stored as
attributes (\code{breaks}, \code{values}, \code{labels}, \code{right}).
}
\description{
Builds a step value schedule: a payoff that is constant within a quality band
and jumps at each breakpoint. This is the canonical shape of a quality-graded
payoff -- a grain price that steps up as protein clears 10.5\%, 11.5\%, and
13\%, say -- where the value of an outcome depends on which band it falls in,
not on its exact magnitude. The returned object is a vectorised function from
a quality outcome to its band payoff, classed so it carries the schedule for
inspection and prints as a readable band table.
}
\details{
\code{breaks} are the band edges, strictly increasing. \code{values} are the payoffs,
one per band: the first applies below \code{breaks[1]}, the last applies at or
above \code{breaks[length(breaks)]}, and each interior element applies between a
consecutive pair of breaks. With \code{right = FALSE} (the default) each band is
the half-open interval \verb{[lower, upper)}, so an outcome exactly on a break
clears into the higher band -- the convention a delivery standard uses when
it pays the better grade at the threshold.
}
\examples{
# An APW-style protein payoff: $/t steps up as protein clears each band.
v <- grade_band_value(breaks = c(10.5, 11.5, 13),
                      values = c(300, 330, 360, 400))
v(c(9.0, 11.0, 11.5, 14.0))
v
}
\seealso{
\code{\link[=grade_band_utility]{grade_band_utility()}} to turn the schedule into a \code{decide()}
utility; \code{\link[=decide]{decide()}} for the engine that consumes it.

Other loss: 
\code{\link{grade_band_utility}()}
}
\concept{loss}
