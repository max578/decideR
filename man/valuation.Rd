% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valuation-class.R
\name{valuation}
\alias{valuation}
\title{A priced quantity with grounding and abstention}
\usage{
valuation(
  value = NA_real_,
  type = NA_character_,
  components = list(),
  grounding = "[unverified]",
  abstained = FALSE,
  abstain_reason = NA_character_,
  baseline = NULL,
  method = NA_character_,
  inputs = list(),
  metadata = list()
)
}
\arguments{
\item{value}{The priced quantity, in the utility units of the inputs
(currency per unit area for a crop valuation, a dimensionless ratio for a
benefit-cost ratio), or \code{NA_real_} when abstained.}

\item{type}{A short valuation type: \code{"evpi"}, \code{"evsi"} or \code{"bcr"}.}

\item{components}{A named list of the parts the \code{value} is assembled from,
so the number can be audited rather than trusted.}

\item{grounding}{A canonical grounding token.}

\item{abstained}{\code{TRUE} when the valuation declined to price the quantity.}

\item{abstain_reason}{A short reason string, or \code{NA_character_}.}

\item{baseline}{The \code{decision} the current evidence supports, for a value of
information; \code{NULL} otherwise.}

\item{method}{A label recording the valuation rule.}

\item{inputs}{A named list recording input provenance.}

\item{metadata}{A named list of free-form metadata.}
}
\value{
A \code{valuation} S7 object.
}
\description{
The object returned by \code{\link[=decide_evpi]{decide_evpi()}}, \code{\link[=decide_evsi]{decide_evsi()}} and \code{\link[=decide_bcr]{decide_bcr()}}.
It records the priced \code{value}, the \code{type} of valuation that produced it, the
arithmetic \code{components} a reader needs in order to audit that value, and the
two Independent Oracle Principle honesty fields: \code{grounding}, and the
abstention record (\code{abstained}, \code{abstain_reason}). When a valuation abstains,
\code{value} is \code{NA_real_} -- decideR never mints a dollar figure on evidence its
producer did not ground.
}
\details{
The \code{abstain_reason} vocabulary extends the \code{decision} vocabulary with the
reasons specific to pricing: \code{input_ungrounded}, \code{no_feasible_action},
\code{low_ess}, \code{degenerate_utility} (all inherited from the decision that the
valuation is built on) and \code{undefined_ratio} (a benefit-cost ratio whose
discounted cost base is not strictly positive).
}
\examples{
v <- valuation(value = 12.4, type = "evpi",
               grounding = grounding_grounded())
v@value
}
\seealso{
\code{\link[=decide_evpi]{decide_evpi()}}, \code{\link[=decide_evsi]{decide_evsi()}}, \code{\link[=decide_bcr]{decide_bcr()}}.

Other value: 
\code{\link{bcr_crrdc}()},
\code{\link{decide_bcr}()},
\code{\link{decide_evpi}()},
\code{\link{decide_evsi}()}
}
\concept{value}
