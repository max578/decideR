% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision-class.R
\name{decision}
\alias{decision}
\title{A decision-theoretic action with grounding and abstention}
\usage{
decision(
  action = NULL,
  action_label = NA_character_,
  expected_utility = NA_real_,
  candidates = NULL,
  grounding = "[unverified]",
  abstained = FALSE,
  abstain_reason = NA_character_,
  safe_action = NULL,
  method = NA_character_,
  inputs = list(),
  metadata = list()
)
}
\arguments{
\item{action}{The chosen action. The \code{safe_action} when \code{abstained} is
\code{TRUE}.}

\item{action_label}{A short human-readable label for the action; suffixed
\code{"[unverified]"} when the grounding is not grounded.}

\item{expected_utility}{The expected (mean-over-draws) utility of the chosen
action, or \code{NA_real_} when abstained.}

\item{candidates}{A \code{data.frame} with one row per candidate action:
\code{action}, \code{expected_utility}, \code{feasible}.}

\item{grounding}{A canonical grounding token.}

\item{abstained}{\code{TRUE} when the decision declined the utility-maximal
action.}

\item{abstain_reason}{A short reason string, or \code{NA_character_}.}

\item{safe_action}{The fallback action taken on abstention.}

\item{method}{The decision rule applied (e.g. \code{"expected_profit"}).}

\item{inputs}{A named list recording input provenance.}

\item{metadata}{A named list of free-form metadata.}
}
\value{
A \code{decision} S7 object.
}
\description{
The object returned by \code{\link[=decide]{decide()}} and the reference decisions. It records
the chosen \code{action}, the \code{expected_utility} that selected it, the complete
\code{candidates} ledger (every action with its expected utility and
feasibility), and the two honesty fields the Independent Oracle Principle
requires: \code{grounding} (\code{grounding_grounded()} or \code{grounding_unverified()})
and the abstention record (\code{abstained}, \code{abstain_reason}). When a decision
abstains the \code{action} is the caller's \code{safe_action}, not the utility-maximal
one.
}
\examples{
d <- decision(action = 120, action_label = "rate=120 kg/ha",
              expected_utility = 1840, grounding = grounding_grounded())
d@action
}
\seealso{
\code{\link[=decide]{decide()}} and the reference decisions that produce this object.

Other decisions: 
\code{\link{decide}()},
\code{\link{decide_input_rate}()},
\code{\link{decide_position_size}()},
\code{\link{is_grounded}()}
}
\concept{decisions}
