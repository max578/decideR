% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide.R
\name{decide}
\alias{decide}
\title{Choose an action by expected utility, with grounding and abstention}
\usage{
decide(
  draws,
  utility,
  candidates,
  labels = NULL,
  constraint = NULL,
  grounding = grounding_unverified(),
  safe_action = NULL,
  safe_label = "abstain",
  min_ess = NULL,
  ess = NULL,
  decisive_prob = 0.6,
  method = "expected_utility",
  inputs = list(),
  metadata = list()
)
}
\arguments{
\item{draws}{A numeric vector of posterior draws of the latent state.}

\item{utility}{A function \verb{function(action, theta)} returning a numeric
vector of utilities the same length as \code{theta}.}

\item{candidates}{A numeric vector (or list) of candidate actions.}

\item{labels}{Optional character labels for the candidates; defaults to a
formatted \code{candidates}.}

\item{constraint}{Optional \verb{function(action)} returning \code{TRUE} when the
action is feasible.}

\item{grounding}{One or more grounding tokens for the inputs; combined
worst-case.}

\item{safe_action}{The action returned on abstention (e.g. a flat position,
a zero input rate, the status quo).}

\item{safe_label}{A label for the safe action.}

\item{min_ess}{Optional effective-sample-size floor.}

\item{ess}{Optional effective sample size of \code{draws}; defaults to
\code{length(draws)}.}

\item{decisive_prob}{The minimum posterior probability that the chosen action
beats \code{safe_action}, below which the decision abstains as not clearly
better than the status quo.}

\item{method}{A label recording the decision rule.}

\item{inputs}{A named list recording input provenance.}

\item{metadata}{A named list of free-form metadata.}
}
\value{
A \link{decision} object.
}
\description{
The shared decision rule. Given \code{draws} from the posterior of a latent state
\eqn{\theta}, a \code{utility} function \eqn{U(a, \theta)}, and a set of candidate
actions, \code{decide()} evaluates the expected utility
\eqn{E_\theta[U(a, \theta)]} of each feasible candidate by averaging over the
draws and selects the maximiser. It declines to do so -- abstaining to
\code{safe_action} -- under any of four conditions, checked in this order: the
combined input \code{grounding} is not grounded (the Independent Oracle Principle
firewall: never act on an unverified fact); no candidate satisfies
\code{constraint}; the effective sample size is below \code{min_ess}; or the chosen
action beats the \code{safe_action} on fewer than \code{decisive_prob} of the draws
(it is not clearly better than the status quo). The last check is judged
against the safe action, not the runner-up, so a fine candidate grid does
not trigger spurious indecision.
}
\details{
The grounding label is combined worst-case across \code{grounding} with
\code{\link[=combine_grounding]{combine_grounding()}}, so passing the provenance of several inputs taints the
decision if any one of them is unverified.
}
\examples{
set.seed(1L)
theta <- rnorm(2000L, mean = 1.2, sd = 0.4)
u <- function(a, theta) -(a - theta)^2
decide(theta, u, candidates = seq(0, 3, by = 0.5),
       grounding = grounding_grounded(), safe_action = 0)
}
\seealso{
\code{\link[=decide_input_rate]{decide_input_rate()}} and \code{\link[=decide_position_size]{decide_position_size()}} for domain
wrappers; \link{decision} for the returned object.

Other decisions: 
\code{\link{decide_input_rate}()},
\code{\link{decide_position_size}()},
\code{\link{decision}()},
\code{\link{is_grounded}()}
}
\concept{decisions}
