% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grounding.R
\name{combine_grounding}
\alias{combine_grounding}
\title{Combine input grounding worst-case}
\usage{
combine_grounding(tokens)
}
\arguments{
\item{tokens}{A character vector of grounding tokens, each either
\code{grounding_grounded()} or \code{grounding_unverified()}.}
}
\value{
\code{grounding_grounded()} if \code{tokens} is non-empty and every element is
grounded; otherwise \code{grounding_unverified()}.
}
\description{
A decision that rests on several inputs is grounded only when \emph{every} input
is grounded; a single un-grounded input taints the whole decision. This is
the worst-case combination mandated by the Independent Oracle Principle, and
it is the rule by which a decision inherits its grounding label from the
facts it consumes.
}
\details{
Each token is validated against the canonical vocabulary, so a synonym such
as \code{"unverified"} or \code{"ungrounded"} is rejected loudly rather than silently
mistaken for a grounded fact.
}
\examples{
combine_grounding(c(grounding_grounded(), grounding_grounded()))
combine_grounding(c(grounding_grounded(), grounding_unverified()))
}
\seealso{
\link{grounding_tokens} for the two canonical tokens.

Other grounding: 
\code{\link{grounding_tokens}}
}
\concept{grounding}
